[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -q
# Without this, bare `pytest` (unlike `python -m pytest`) doesn't add the cwd to
# sys.path, so top-level packages here (utils/, ca/, scoring/, ...) fail to import.
pythonpath = .
